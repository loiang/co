Overall:
- Why: 当前真实 Codex archive 的 `bin/codex` member size 为 1,295,324,608 bytes，超过原有 1 GiB 单成员上限，导致合法 release archive 被拒绝。
- Goal: 将单成员边界提升到 2 GiB，同时保留 4 GiB package 总量边界，并让单成员大小错误与路径、类型、重复项错误分别报告。
- Accomplished: validator 使用 2 GiB member limit 和 4 GiB package limit；新增 metadata-only 回归测试验证约 1.3 GiB `bin/codex` 接受、超过 2 GiB 拒绝，并更新 archive 超限边界测试。验证结果：目标测试 39 passed，全量 `test/co` 147 passed/2 skipped，Ruff 与 `git diff --check` 通过。
